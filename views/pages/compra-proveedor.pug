extends ../layout 

block head 
  link(rel="stylesheet" href="/stylesheets/registros.css")

block content
  .container.mt-5.mb-5
  .col-md-10
    h5= message
    form.form.row(method="POST" action="/compra/agregar")
      input(type="hidden" name="prID" value=`${ prID }` readonly) 
      input(type="hidden" name="prNombre" value=`${ prNombre }` readonly)
      .col-3
        input.form-control(type="submit" class='btn btn-dark' value="Volver" )
    div(class="card mt-4")
      div(class="card-header")
        span 
          i(class="bi bi-table me-2") Seleccionar Proveedor
      div(class="card-body")
        div(class="table-responsive")
          table(id="tbl" class="table table-striped" style="width: 100%")
            thead
              tr
                th NOMBRE
                th DIRECCION
                th TELEFONO
                th ACCION
            tbody
              each val in data 
                tr(id=`${val.id}`) 
                  td= val.nombre
                  td= val.direccion
                  td= val.telefono
                  td 
                    form(method="POST" action="/compra/agregar")
                      input.form-control(type="submit" class='btn btn-outline-success' value="Agregar" )
                      input(type="hidden" name="prID" value=`${ val.id }` readonly) 
                      input(type="hidden" name="prNombre" value=`${ val.nombre }` readonly)
            tfoot

block footer
  script(src="/javascripts/newDataTable.js")